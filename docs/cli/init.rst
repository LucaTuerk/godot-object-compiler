init
====

The init command interactively initializes a godot-object-compiler project file in the current directory.
The project file workflow can be used to avoid repeating the project specific arguments when using
the CLI directly, when invoked automatically via the build system is is not needed.

**Help Text:**

.. include:: descriptions/init.rst

.. toctree::
    :name: cli_init

    init_tools
    init_local_resources
