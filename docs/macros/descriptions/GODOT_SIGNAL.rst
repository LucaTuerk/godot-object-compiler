Mark the following function as a signal prototype.

The function must have a void return type and all parameters must be Godot Variant
compatible types.

The marked function must not be implemented, an implementation which can be used to
emit the signal will be generated.